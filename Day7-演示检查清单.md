# Day7 演示检查清单

## 1. 环境检查（每轮彩排前）

- [ ] Docker 服务已启动（WSL）
- [ ] 执行：`wsl -e bash -lc "cd /mnt/d/AI智学体 && docker compose up -d"`
- [ ] 前端可访问：`http://localhost:3000`（HTTP 200）
- [ ] 后端健康：`http://localhost:8000/health`（HTTP 200，postgres/redis=ok）

## 2. 场景 A（新用户首次使用）

- [ ] 使用 `13800000001 / 8888` 登录
- [ ] 首页展示目标院校、差距分、倒计时
- [ ] 可进入“打开艾乐”对话页
- [ ] 计划列表接口返回至少 1 条数据

## 3. 场景 B（日清对话）

- [ ] 输入：`今天数学课讲了三角函数，有个地方没听懂`
- [ ] 对话中显示“艾乐正在思考...”
- [ ] 当等待超过 10 秒，出现“艾乐正在思考，请稍候...”
- [ ] 可看到 SSE 返回 token/metadata/done 事件
- [ ] 可从对话跳转到执行页

## 4. 场景 C（诊断报告）

- [ ] 进入诊断页可看到薄弱知识点列表
- [ ] “生成巩固计划”可成功调用并返回计划
- [ ] 成功跳转执行页并可看到新生成任务

## 5. 稳定性检查

- [ ] 前端 ErrorBoundary 已接入（异常不白屏，可返回首页）
- [ ] Docker 冷启动时无端口冲突（postgres/redis 不占用宿主端口）
- [ ] backend 容器健康检查为 `healthy`

## 6. 第 1 轮彩排记录（已执行）

- 结论：A/C 场景通过；B 场景在未配置 `LLM_API_KEY` 时可返回兜底回复，但自动任务创建未稳定触发。
- 已修复项：
  - 前端 ErrorBoundary 已接入
  - 对话 10 秒慢响应提示已接入
  - Docker 构建安装依赖增加重试与超时参数
  - Docker 端口冲突问题已规避
  - backend 健康检查从 `curl` 改为 Python 内置请求
- 待确认项（第 2 轮重点）：
  - 若需稳定演示 B 场景“自动创建任务”，建议开启 `VITE_DEMO_MODE=true` 或配置有效 `LLM_API_KEY`

## 7. 第 2 轮建议动作

- [x] 启用演示模式（推荐）
  - `frontend/.env` 配置：`VITE_DEMO_MODE=true`
  - 重建前端：`wsl -e bash -lc "cd /mnt/d/AI智学体 && docker compose up -d --build frontend"`
- [x] 按 A/B/C 全流程走一遍并记录卡点
- [x] 若仍有卡点，优先修复 P0（阻塞演示）

## 8. 第 2 轮彩排记录（已执行）

- 结论：A/C 场景稳定通过；B 场景在演示模式下具备稳定可演示路径。
- 关键验证：
  - `frontend/.env` 已启用 `VITE_DEMO_MODE=true`
  - 前端已重建并上线（`http://localhost:3000` 可访问）
  - 演示任务 ID `c0000000-0000-0000-0000-000000000003` 可正常打开，含 1 个知识点与 1 道练习题
- 当前建议：第 3 轮进入正式计时彩排（15-20 分钟），优先使用 Day 6 脚本原话触发 B 场景演示链路。

## 9. 第 3 轮（正式计时）

- [ ] 按正式讲解脚本完整走通
- [ ] 计时控制在 15-20 分钟
- [ ] 同步准备备用视频与应急口径
