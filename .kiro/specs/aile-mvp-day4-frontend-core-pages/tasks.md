# 实施计划：艾乐学伴 MVP Day 4 — 前端核心页面开发

## 概述

将 Day 4 设计方案拆解为可执行开发任务，按“公共基础 -> 登录 -> 主界面 -> 对话 -> 学习计划 -> 联调验证”推进。  
按你的要求，当前先产出任务清单，待你确认后再进入代码提交阶段。

## 任务

- [x] 1. 搭建 Day 4 公共类型与服务层
  - [x] 1.1 定义 API 类型与页面数据模型
    - 在 `frontend/src/types/` 新增 `auth.ts`、`chat.ts`、`plan.ts`（或合并为 `api.ts`）
    - 覆盖登录响应、用户信息、会话消息、计划与任务结构
    - 对 SSE 事件结构定义联合类型（`token`、`metadata`、`done`、`error`）
    - _需求: 1.2, 3.3, 5.4, 6.4, 7.1, 8.3_

  - [x] 1.2 新增业务服务封装
    - 新建 `services/authService.ts`：登录、获取用户、更新画像
    - 新建 `services/chatService.ts`：SSE 消息发送、会话列表、消息历史
    - 新建 `services/planService.ts`：计划列表/详情、任务列表
    - _需求: 1.2, 2.3, 3.3, 5.3, 6.1, 8.3_

- [x] 2. 扩展全局状态管理（Zustand）
  - [x] 2.1 升级 `useUserStore`
    - 增加登录动作、用户信息拉取、画像补全状态字段
    - 启动时从 `localStorage` 恢复 token 并更新 `isAuthenticated`
    - _需求: 1.3, 1.4, 1.6, 2.4, 9.1, 9.2_

  - [x] 2.2 新增对话与计划状态 store
    - 新建 `stores/useChatStore.ts` 管理 `sessionId`、消息列表、流式状态、任务提示
    - 新建 `stores/usePlanStore.ts` 管理当前计划、任务分组、聚焦任务
    - _需求: 5.1, 5.5, 6.3, 7.1, 8.2_

- [x] 3. 实现登录页（`/auth/login`）
  - [x] 3.1 替换占位登录页为业务表单
    - 实现手机号、验证码输入与按钮状态控制
    - 接入 `POST /api/v1/auth/login` 登录流程
    - _需求: 1.1, 1.2, 1.5_

  - [x] 3.2 实现首次登录画像引导
    - 当 `is_new_user=true` 时展示引导区域（年级、教材版本、目标院校）
    - 调用 `PUT /api/v1/users/me` 保存画像并回拉用户信息
    - _需求: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.3 登录后导航与守卫
    - 登录成功统一跳转 `/`
    - 未登录访问业务页时重定向到 `/auth/login`
    - _需求: 1.4, 4.4, 9.1_

- [x] 4. 实现主界面（`/`）
  - [x] 4.1 构建主界面布局与视觉结构
    - 顶部目标区、中心任务卡、艾乐入口、我的计划入口
    - 保持 Day 4 极简白板风格（大留白、聚焦任务）
    - _需求: 3.1, 4.1, 4.2_

  - [x] 4.2 接入用户/计划/任务数据
    - 拉取用户信息和计划任务数据，推导当前展示任务
    - 处理 loading/error/empty 三态
    - _需求: 3.2, 3.3, 3.4, 3.5, 3.6, 9.3_

  - [x] 4.3 完成主界面跳转行为
    - 艾乐入口 -> `/daily-clearance`
    - 我的计划、任务按钮 -> `/execution`（携带任务上下文）
    - _需求: 4.1, 4.2, 4.3_

- [x] 5. 实现艾乐对话页（`/daily-clearance`）
  - [x] 5.1 构建消息气泡 UI 与输入区
    - 用户消息右侧、艾乐消息左侧，显示头像与名称
    - 输入区支持发送中禁用与回车发送
    - _需求: 5.1, 5.2_

  - [x] 5.2 接入 SSE 流式对话
    - 调用 `POST /api/v1/chat/message` 并解析流式事件
    - 增量渲染 token，完成 done 收口与异常处理
    - _需求: 5.3, 5.4, 5.5, 5.6_

  - [x] 5.3 实现历史会话恢复
    - 进入页面拉取会话列表和最近会话消息
    - 保证消息时间正序与会话上下文连续
    - _需求: 6.1, 6.2, 6.3, 6.4_

  - [x] 5.4 实现任务创建提示卡片
    - 监听 metadata 事件中的 `task_created`
    - 渲染“已为你创建学习任务”卡片并提供跳转 `/execution`
    - _需求: 7.1, 7.2, 7.3, 7.4_

- [x] 6. 实现学习计划页（`/execution`）
  - [x] 6.1 替换占位页为计划与任务视图
    - 展示计划标题、状态、任务统计信息
    - _需求: 8.1_

  - [x] 6.2 接入计划任务数据并按状态分组
    - 调用 `GET /api/v1/plans`、`GET /api/v1/tasks`
    - 分组展示 `in_progress` / `pending` / `completed`
    - _需求: 8.2, 8.3_

  - [x] 6.3 渲染任务卡片字段与空态
    - 展示标题、类型、知识点标签、预计时长
    - 无计划/无任务时给出引导
    - _需求: 8.4, 8.5_

- [x] 7. 路由守卫与页面联动完善
  - [x] 7.1 在 `App.tsx` 增加登录守卫组件（或等效方案）
    - 保护 `/`、`/daily-clearance`、`/execution`
    - 保持 `/auth/login` 为公开路由
    - _需求: 9.1, 9.2_

  - [x] 7.2 对齐现有 `AppLayout` 导航与页面文案
    - 导航标签与 Day 4 页面语义一致
    - 避免占位页命名与真实功能冲突
    - _需求: 4.1, 4.2_

- [x] 8. 联调验证与质量检查
  - [x] 8.1 执行 Day 4 闭环手工验证
    - 登录 -> 主界面 -> 对话流式 -> 任务提示 -> 计划页
    - _需求: 10.1, 10.2, 10.3, 10.4, 10.5_

  - [x] 8.2 执行基础质量检查
    - 运行 `npm run build`（frontend）确保类型与构建通过
    - 使用 `GetDiagnostics` 检查 Day 4 修改文件并修复可定位问题
    - _需求: 9.3, 9.4_

## 备注

- 本文件已按 Day 4 实际落地结果回写为“已执行完成”状态。
- 代码与文档已对齐，前端 `npm run build` 通过，核心闭环可运行。
