# 实施计划：艾乐学伴 MVP Day 5 — 学习闭环前端 + 前后端联调

## 概述

将 Day 5 设计方案拆解为可执行任务，按“执行页升级 -> 练习与状态机 -> 对话联动 -> 诊断简化 -> 联调验证”推进。  
当前文档已根据实际开发与验证结果回写状态。

## 任务

- [x] 1. 补齐 Day 5 类型与服务能力
  - [x] 1.1 扩展前端类型定义
    - 在 `frontend/src/types/api.ts`（或拆分文件）补充 TaskDetail、讲解分段、练习提交结果相关类型
    - 增加执行页上下文类型与任务完成判定类型
    - _需求: 1.2, 3.1, 4.3, 5.1_

  - [x] 1.2 扩展计划服务层能力
    - 在 `frontend/src/services/planService.ts` 增加 `getTaskDetail(taskId)` 与必要的答题提交封装
    - 保持与现有接口一致：`GET /tasks/{id}`、`POST /tasks/{id}/submit-answer`、`PUT /tasks/{id}/status`
    - _需求: 1.2, 2.1, 4.3, 5.1_

- [x] 2. 升级执行页为学习闭环页（`/execution`）
  - [x] 2.1 实现任务聚焦与详情加载
    - 从任务列表进入聚焦任务态，加载任务详情
    - 处理无任务、加载失败与重试
    - _需求: 1.1, 1.2, 1.3, 1.4_

  - [x] 2.2 实现任务开始自动流转
    - 对 `pending` 任务进入详情时自动更新为 `in_progress`
    - 成功后更新 store 与 UI 标识
    - _需求: 2.1, 2.2, 2.3_

  - [x] 2.3 实现讲解区三段展示
    - 按“概念 -> 例题 -> 总结”渲染讲解内容
    - 增加内容缺失时的保底展示
    - _需求: 3.1, 3.2_

  - [x] 2.4 实现“我有疑问”跳转行为
    - 从执行页跳转 `/daily-clearance`，携带任务/知识点上下文
    - _需求: 3.3_

- [x] 3. 实现练习题作答与判定反馈
  - [x] 3.1 实现选择题与填空题输入交互
    - 支持选项点击、输入框填写、提交前校验
    - _需求: 4.1, 4.2_

  - [x] 3.2 接入提交答案接口与结果渲染
    - 调用 `POST /tasks/{id}/submit-answer` 并渲染正误与解析
    - 异常时保留输入并可重试
    - _需求: 4.3, 4.4, 4.5_

  - [x] 3.3 实现任务完成状态更新
    - 达成完成条件后调用 `PUT /tasks/{id}/status` 更新为 `completed`
    - 刷新任务分组并回到列表态
    - _需求: 5.1, 5.2, 5.3_

- [x] 4. 打通对话到执行页联动
  - [x] 4.1 对齐任务创建提示跳转参数
    - 在 `DailyClearancePage` 与 `useChatStore` 间统一 `task_id` 透传
    - _需求: 6.1, 6.2_

  - [x] 4.2 执行页消费跳转上下文
    - 优先加载指定任务，失效时回退默认策略
    - _需求: 6.2, 6.3_

- [x] 5. 实现诊断页简化版（`/diagnosis`）
  - [x] 5.1 输出薄弱知识点列表与空态
    - 接入可用数据源或提供演示占位映射
    - _需求: 7.1, 7.3_

  - [x] 5.2 增加“生成巩固计划”按钮
    - 先接已存在页面/行为，保证可点击可反馈
    - _需求: 7.2_

- [ ] 6. 联调与质量验证
  - [ ] 6.1 执行 Day 5 主链路联调（Docker 环境复验）
    - 登录 -> 对话生成任务 -> 执行学习 -> 提交练习 -> 完成任务
    - 当前阻塞：本机缺少 `docker` 命令，未完成真实全栈端到端复验
    - 已完成替代验证：前端 `npm run build` 通过；后端关键链路测试 `11 passed`
    - _需求: 8.1, 8.2, 8.3, 10.1_

  - [x] 6.2 修复联调关键问题
    - 处理字段映射、状态同步、空数据与异常场景
    - _需求: 8.4, 9.3, 10.3_

  - [x] 6.3 验证鉴权与回归
    - 校验 401 跳转、受保护路由、Day 4 既有链路不回退
    - _需求: 9.1, 9.2, 10.3_

  - [x] 6.4 执行构建与诊断检查
    - 运行前端构建与必要检查，修复可定位问题
    - _需求: 10.2, 10.3_

## 当前门禁

- 已完成代码提交与可执行验证。
- 仅剩 `6.1` 真实全栈联调待在具备 Docker 环境的机器上复验。
